Create following secrets in aws secret manager:
 - GitHubAuth -> Key-value secret -> Key = token, value = xxx
 
When running create-stack:
create-stack --template-body "serverless-template.yml" --capabilities "CAPABILITY_NAMED_IAM" --parameters ParameterKey=DbPasswordParam,ParameterValue=myDbPassword